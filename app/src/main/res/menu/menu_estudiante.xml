<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/icon_nuevo"
        android:icon="@drawable/ic_nuevo"
        android:title="Nuevo"
        app:showAsAction="always">
    </item>
    <item android:id="@+id/icon_guardar"
        android:icon="@drawable/ic_guardar"
        android:title="Guardar"
        app:showAsAction="always">
    </item>
    <item android:id="@+id/icon_eliminar"
        android:icon="@drawable/ic_eliminar"
        android:title="Eliminar"
        app:showAsAction="always">
    </item>
    <item android:id="@+id/icon_regresar"
        android:icon="@drawable/ic_regresar"
        android:title="Regresar"
        app:showAsAction="always">
    </item>
</menu>